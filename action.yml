# action.yml
name: 'Buildfish'
description: 'Build SailfishOS apps with Docker'
inputs:
  release:  # id of input
    description: 'SailfishOS release'
    required: true
    default: 'latest'
outputs:
  rpm: # id of output
    description: '???'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.release }}
